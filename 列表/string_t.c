#include <stdio.h>
#include <stdio.h>
#include <string.h>
int main(int argc, char const *argv[])
{
    int x;
    char temp[10];
    while (scanf("%s", temp))
    {
        printf("yes\n");
    }
    fflush(stdin);
switch (expression)
{
case /* constant-expression */:
    {/* code */}
    break;

default:
    break;
}

    getchar(); //this still have some data in the buffer!!
    return 0;
}
int vv(void)
{
    return 19;
}